#include "MrText/Event.hpp"
#include <csignal>
#include <iostream>

// auto Event::loop(const KeyMapData &data, buffer_type *buffer) noexcept -> void {
//   if (buffer != nullptr) {
//     while (read(STDIN_FILENO, buffer, 4) != -1) {
	  
//     }
//   } else {
//     std::cerr << "Something happened to the read buffer."
//               << "\n";
//   }
// }
